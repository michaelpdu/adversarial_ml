function onComponentEndInstall509() {
    var clickid = get_hardcode_define('clickid');
    if (clickid != '' && clickid != 'undefined') {
        var res = do_get('http://clkads.com/adServe/track?prdid=39&subid=' + clickid);
    }
}